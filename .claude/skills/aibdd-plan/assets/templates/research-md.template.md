# Planning Research

> Generated by `/aibdd-plan`. This records reasoning and trade-offs for this planning session.

## Inputs Considered

<!-- INSTRUCT: Point to Discovery sourcing, activities, features, boundary truth, code skeleton and relevant existing truth. -->

## Decisions

<!-- INSTRUCT: Record technical choices with rationale and rejected alternatives.
Include no sentence-parts inventory decision when backend handler-routing is the
SSOT, shared DSL template materialization, and whether DSL key locale was
resolved deterministically. -->

## External Dependencies

<!-- INSTRUCT: Record provider, third-party, mock/stub and failure-mode trade-offs. -->

## Internal Implementation Notes

<!-- INSTRUCT: Record collaboration choices and implementation risks without writing product code.
Explain how major happy/alt/err behavior paths were split into sequence diagrams
using `<scenario>.<category>.sequence.mmd`. -->

## DSL Design Notes

<!-- INSTRUCT: Record why entries are local vs shared, why preset handlers were chosen, and how bindings were derived.
Mention stateless DSL identity exposure, dynamic ID policy, no raw technical
datatable payloads, nested aggregate business projection, and default binding
justification when present. -->
