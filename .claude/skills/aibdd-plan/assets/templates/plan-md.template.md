# Technical Plan

> Generated by `/aibdd-plan`. This file is a plan-package artifact, not truth SSOT.

## Scope

<!-- INSTRUCT: Summarize the Discovery scope and target function package pointer. -->

## Reconciliation Context

<!-- INSTRUCT: If `reconcile_context` exists, render trigger description, earliest planner, cascade chain, and archive path as a short narrative block at the top of the plan. If `reconcile_context` is empty, write one concise line that this is a forward planning run without reconciliation context. -->

## Impacted Feature Files

<!-- INSTRUCT: List the feature files impacted by the current plan package in downstream TDD order.
Use one bullet per canonical repo-relative feature path. When the plan covers the full function
package, list the full ordered feature set. A short rationale may follow each path. -->

## Technical Boundary Decisions

<!-- INSTRUCT: Summarize boundary-map, contract, data/state and test strategy decisions by pointer only. Do not duplicate full truth content. -->

## External Boundary Surface

<!-- INSTRUCT: List provider boundaries, third-party dependencies, operation contracts, and test double policy pointers. -->

## Internal Implementation Strategy

<!-- INSTRUCT: Summarize implementation shape and list every sequence diagram path.
Use one bullet per `<scenario>.<category>.sequence.mmd`; do not collapse major
happy/alt/err paths into one total-flow diagram. Keep paths as pointers only. -->

## DSL Truth Summary

<!-- INSTRUCT: List local/shared DSL entries added or reused, with pointers and no shadow copies. -->

## Remediation / Blocking Gaps

<!-- INSTRUCT: List planning gaps that must be resolved before downstream analysis. -->
