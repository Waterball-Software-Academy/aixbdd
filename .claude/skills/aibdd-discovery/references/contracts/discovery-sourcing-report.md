# Discovery Sourcing Report Contract

`reports/discovery-sourcing.md` is a plan-session report owned by `/aibdd-discovery`.

## Required Sections

| Section | Content |
|---|---|
| `# Discovery Sourcing` | title |
| `## Scope` | raw idea pointer, target boundary pointer, target function package pointer |
| `## Evidence Matrix` | rows shaped by `references/relevance.md` §2 |
| `## Impact Scope` | normalized flow / rule / actor / state / operation / dependency impacts |
| `## Gaps` | blocking and non-blocking gaps |

## Hard Rules

- Do not copy truth content. Use pointers only.
- Do not write delta / diff. Remediation belongs to later plan artifacts.
- Do not make this report a second DSL / contract / data model SSOT.
- `spec.md` may summarize this report but must link back to it instead of duplicating the matrix.
